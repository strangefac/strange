package com.github.strangefac.strange.impl;

import com.github.strangefac.strange.Wrapper;

class WrapperImpl<V, E extends Throwable> extends SFutureImpl<V, E> implements Wrapper<V, E> {
  // Nothing else.
}
