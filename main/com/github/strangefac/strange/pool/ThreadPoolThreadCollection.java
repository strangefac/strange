package com.github.strangefac.strange.pool;

import com.github.strangefac.strange.ThreadCollection;

/** All non-swing actors can share one of these. */
public interface ThreadPoolThreadCollection extends ThreadCollection {
  // Nothing else.
}
